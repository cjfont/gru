{
  "name": "gru",
  "version": "0.0.1",
  "description": "gru is a git wrapper that allows a project's files and directories to be spread across multiple repositories but managed as one",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/cjfont/gru"
  },
  "dependencies": {
    "async": "^0.9.0",
    "fs-extra": "^0.16.5",
    "js-yaml": "^3.2.7",
    "lodash": "^3.5.0",
    "minimist": "^1.1.1"
  }
}